import MoviesList from "../MovieList";

const MainScreen = () => {
  return (
    <div className="main-screen-container">
      <MoviesList />
    </div>
  );
};

export default MainScreen;
